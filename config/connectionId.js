const port = require('./web-server');

const connIdentifiers={
	    clientId: "rnatarajn",
	    clientInfo: `running on port ${port.port}`,
	    module: "DHLServices",
	    action: "Item"
	  }
  module.exports=connIdentifiers;
